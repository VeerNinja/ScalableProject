apiVersion: v1
kind: Service
metadata:
  name: {{ include "scalableproject.fullname" . }}
  labels:
    {{- include "scalableproject.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scalableproject.selectorLabels" . | nindent 4 }}

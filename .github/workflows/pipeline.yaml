name: CI/CD Pipeline

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build OrderService
        run: mvn -B clean package
        working-directory: ./OrderService

      - name: Build UserService
        run: mvn -B clean package
        working-directory: ./UserService

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Images
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/order-service:latest ./OrderService
          docker push ${{ secrets.DOCKER_USERNAME }}/order-service:latest
          docker build -t ${{ secrets.DOCKER_USERNAME }}/user-service:latest ./UserService
          docker push ${{ secrets.DOCKER_USERNAME }}/user-service:latest

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: '1.27.3'

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_CONTENT }}" > ~/.kube/config

      - name: Deploy with Helm
        run: |
          helm upgrade --install scalableproject ./scalableproject --namespace default --wait
